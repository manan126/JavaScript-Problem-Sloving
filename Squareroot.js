const number = 16
if (number < 0) {
    console.log('Square root of a negative number is not real.');
} else {
    const squareRoot = Math.sqrt(number);
    console.log(`The square root of ${number} is ${squareRoot}.`);
}